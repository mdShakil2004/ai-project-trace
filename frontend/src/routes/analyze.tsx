import { useEffect, useState } from "react";
import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { AlertTriangle, Check, Github, Loader2, Sparkles } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { PageHeader } from "@/components/trace/Primitives";
import { api } from "@/services/api";
import type { AnalysisEvent } from "@/types";
import { cn } from "@/lib/utils";

export const Route = createFileRoute("/analyze")({ head:()=>({meta:[{title:"Analyze a pull request — Trace"},{name:"description",content:"Give Trace a GitHub pull request and Trace reconstructs the change, intent, impact and verification gaps."},{property:"og:title",content:"Analyze a pull request — Trace"}] }), component:AnalyzePage });

type Phase="idle"|"running"|"error";
const emptyStages:AnalysisEvent[]=[1,2,3,4,5,6,7,8].map((n)=>({id:`st_${n}`,label:["Connecting to GitHub","Fetching pull request","Reading changed files","Mapping dependencies","Recovering historical context","Assessing architectural impact","Identifying risks","Checking verification"][n-1],status:"pending"}));

function AnalyzePage(){
 const navigate=useNavigate(); const [url,setUrl]=useState(""); const [phase,setPhase]=useState<Phase>("idle"); const [events,setEvents]=useState<AnalysisEvent[]>(emptyStages); const [error,setError]=useState<string|null>(null); const [investigationId,setInvestigationId]=useState<string|null>(null); const [realtimeToken,setRealtimeToken]=useState<string|null>(null); const [started,setStarted]=useState<number|null>(null); const [elapsed,setElapsed]=useState(0);
 useEffect(()=>{if(phase!=="running"||!investigationId||!realtimeToken)return;let cancelled=false;let fallbackTimer:ReturnType<typeof setTimeout>|undefined;let unsubscribe:undefined|(()=>void);let usingFallback=false;
 const finish=async()=>{try{await navigate({to:"/investigation/$id",params:{id:investigationId}})}catch(e){if(!cancelled){setError(e instanceof Error?e.message:"Unable to open investigation");setPhase("error")}}};
 const poll=async()=>{try{const next=await api.getEvents(investigationId);if(!cancelled)setEvents(next.length?next:emptyStages);await api.getInvestigation(investigationId);if(!cancelled)await finish();}catch(e){const message=e instanceof Error?e.message:"";if(message&&message!=="Investigation is still analyzing"&&!cancelled){setError(message);setPhase("error");return;}fallbackTimer=setTimeout(poll,1000)}};
 const startRealtime=()=>{unsubscribe=api.subscribeInvestigation(investigationId,realtimeToken,{onEvents:next=>{if(!cancelled)setEvents(next.length?next:emptyStages)},onComplete:()=>{void finish()},onError:()=>{if(!usingFallback&&!cancelled){usingFallback=true;void poll()}}})};
 startRealtime(); return()=>{cancelled=true;unsubscribe?.();if(fallbackTimer)clearTimeout(fallbackTimer)}},[phase,investigationId,realtimeToken,navigate]);
 useEffect(()=>{if(!started){setElapsed(0);return;}const t=setInterval(()=>setElapsed(Math.floor((Date.now()-started)/1000)),1000);return()=>clearInterval(t)},[started]);
 const start=async()=>{const value=url.trim();setError(null);setEvents(emptyStages);if(!value){setPhase("error");setError("Enter a GitHub pull request URL.");return;}try{const result=await api.analyzePullRequest(value);setInvestigationId(result.investigationId);setRealtimeToken(result.realtimeToken);setStarted(Date.now());setPhase("running")}catch(e){setPhase("error");setError(e instanceof Error?e.message:"Analysis failed.")}};
 return <AppShell><PageHeader title="Analyze a Pull Request" subtitle="Give Trace a GitHub PR and we'll reconstruct the change, its intent, impact, and verification gaps."/><div className="mx-auto max-w-3xl space-y-6 px-4 py-8 md:px-6">{phase!=="running"&&<section className="panel p-5"><label htmlFor="pr-url" className="text-xs font-medium">GitHub Pull Request URL</label><div className="mt-2 flex flex-col gap-2 sm:flex-row"><input id="pr-url" value={url} onChange={e=>setUrl(e.target.value)} onKeyDown={e=>e.key==="Enter"&&void start()} placeholder="https://github.com/owner/repo/pull/123" className="mono min-w-0 flex-1 rounded-md border border-border bg-background px-3 py-2 text-xs outline-none focus:border-border-strong"/><button type="button" disabled={!url.trim()} onClick={()=>void start()} className="inline-flex items-center justify-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground disabled:opacity-50"><Sparkles className="size-4"/>Analyze PR</button></div><p className="mt-3 text-[11px] text-muted-foreground">Trace reads the diff, linked discussion, git history and checks. Nothing is written back to the repository.</p></section>}{phase==="error"&&<section className="panel border-risk-high/30 p-5"><h2 className="flex items-center gap-2 text-sm font-medium text-risk-high"><AlertTriangle className="size-4"/>We couldn't analyze this pull request.</h2><p className="mt-2 text-xs text-muted-foreground">{error}</p><div className="mt-4 flex gap-2"><button type="button" onClick={()=>void start()} className="rounded-md bg-primary px-3 py-1.5 text-xs text-primary-foreground">Try again</button><button type="button" onClick={()=>setPhase("idle")} className="rounded-md border border-border px-3 py-1.5 text-xs">Edit URL</button></div></section>}{phase==="running"&&<section className="panel p-5"><header className="flex items-center justify-between border-b border-border pb-3"><h2 className="mono text-sm">Analyzing pull request</h2><span className="mono text-xs text-muted-foreground">{elapsed}s</span></header><ol className="mt-4 space-y-3">{events.map((item)=>{const state=item.status;return <li key={item.id} className="flex items-start gap-3"><span className="mt-0.5">{state==="done"&&<Check className="size-4 text-risk-low"/>}{state==="active"&&<Loader2 className="size-4 animate-spin text-intel"/>}{state==="pending"&&<span className="block size-3.5 rounded-full border border-border-strong"/>}</span><span className={cn("min-w-0 text-sm",state==="pending"&&"text-muted-foreground")}>{item.label}{item.detail&&state!=="pending"&&<span className="mono block text-[11px] text-muted-foreground">{item.detail}</span>}</span></li>})}</ol></section>}{phase!=="running"&&<section className="panel border-risk-medium/30 p-5"><h2 className="flex items-center gap-2 text-sm font-medium text-risk-medium"><AlertTriangle className="size-4"/><span>How Trace analyzes a PR</span></h2><p className="mt-2 text-xs leading-relaxed text-muted-foreground">It fetches the pull request, changed files, history, discussion and checks, then uses the configured OpenRouter model to produce evidence-linked change intelligence.</p><div className="mt-3 flex items-center gap-2 text-[11px] text-muted-foreground"><Github className="size-3.5"/>Read-only GitHub access</div></section>}</div></AppShell>;
}
